import 'package:flutter/material.dart';

class JudgeSummaryWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
      ),
      body: Center(
        child: Text(
          '谢谢参与',
          style: TextStyle(fontSize: 50),
        ),
      ),
    );
  }
}
